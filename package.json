{
	"name": "checklist-server",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "node index.js",
		"dev": "nodemon index.js",
		"migrate": "npx knex migrate:latest --env development",
		"migrate:reset": "npx knex migrate:rollback --env development && npx knex migrate:latest --env development"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"connect-timeout": "^1.9.0",
		"cors": "^2.8.5",
		"dotenv": "^16.0.1",
		"express": "^4.18.1",
		"knex": "^2.1.0",
		"knex-paginate": "^3.0.1",
		"pg": "^8.7.3",
		"pg-connection-string": "^2.5.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.16"
	}
}
